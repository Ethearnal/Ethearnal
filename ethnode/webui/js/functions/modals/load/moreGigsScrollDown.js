// Loads more gigs on scroll down
$(window).scroll(function() {
    if($(window).scrollTop() == $(document).height() - $(window).height()) {
        console.log('hello madafaka, I know youre reading this, have a good evening, I need to leave now.');
        // searchQueryDo();
    }
});